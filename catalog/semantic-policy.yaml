schema_version: semantic-policy.v1
minimum_semantic_revision: 7

# Every public catalog must satisfy revision-7 detail-view closure. There are no
# digest-based legacy exceptions: an opaque compound leaf is always rejected.
